# ─────────────────────────────────────────────────────────────
# Requirements for co2_analysis.ipynb / co2_analysis.py
# Versions below are the exact ones the notebook was validated against.
# Install:   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# --- Core analysis stack (required to run the notebook) ---
pandas==3.0.3
numpy==2.4.6
matplotlib==3.11.0
seaborn==0.13.2
scipy==1.17.1
scikit-learn==1.9.0

# --- Notebook execution / viewing (required to run .ipynb) ---
nbformat==5.10.4      # .ipynb read/write
nbclient==0.11.0      # execute notebooks headlessly
ipykernel==7.3.0      # the Python kernel Jupyter talks to
jupyterlab            # optional GUI to open & run the notebook
